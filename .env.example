# Default env file location:
#   $XDG_CONFIG_HOME/helper/.env
# Fallback if XDG_CONFIG_HOME is unset:
#   ~/.config/helper/.env
#
# To use a different env file, set HELPER_ENV_FILE in your shell before running
# tools (for example: `export HELPER_ENV_FILE=/absolute/path/to/.env`).

# Required: path to Coinbase API credentials JSON (do not commit real secrets)
HELPER_COINBASE_CREDENTIALS_PATH=/absolute/path/to/coinbase-credentials.json

# Optional
# HELPER_LOG_LEVEL=INFO
# HELPER_POSTGRES_DATABASE=helper
# HELPER_POSTGRES_USERNAME=postgres
# HELPER_POSTGRES_PASSWORD=postgres
